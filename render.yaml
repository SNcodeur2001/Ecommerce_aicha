services:
  - type: web
    name: lumiere-json-server
    env: node
    plan: free
    buildCommand: pnpm install --frozen-lockfile
    startCommand: pnpm exec json-server --watch db.json --port $PORT --host 0.0.0.0
    envVars:
      - key: NODE_VERSION
        value: 22

